The delimiter for variables in templates.Checks, if the node is accepted for processing.NodeParameterstrue if the node ist accepted; otherwise, false.Cleans string.String to be cleaned.Should carriage-returns be removed by replacing them with blanks?Cleaned string.Gets the requested formats from the given variables.VariablesThe requested formats.Example: In {\em mdtoc:heading} the format {\em mdtoc} is requested for the variable {\em heading} .Reads all template-files in a directory.Directory with template-files.A dictionary with the templates read.The keys of the dictionary are the filenames (without extension) of the template-files. The keys in turn are the names of nodes or attributes in the XML-file.Removes unused variables from the given text.This is a remark.Replaces word in text.TextWordReplacementOptionsString with word replaced.